name: CI

on:
  push:
    branches: ["master", "developer"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd="mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      MONGO_URI_TEST: mongodb://localhost:27017
      DB_NAME_TEST: my_super_library_test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Por si no están en requirements (pero tú dijiste que ya los tienes instalados)
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        run: pytest -q

  docker_build:
    needs: test
    runs-on: ubuntu-latest
    # Solo build en main (ajusta a tu rama real)
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ✅ OPCIONAL: login a GHCR para push
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend.Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/my-super-library:latest
            ghcr.io/${{ github.repository_owner }}/my-super-library:${{ github.sha }}
